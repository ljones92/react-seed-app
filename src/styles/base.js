// Global base styles in here

import { css } from '@emotion/core';

const base = css`
    html,
    body {
        font-size: 14px;
        font-family: Expletus Sans, cursive;
    }
    body {
        background: #dbdcd7;
        min-height: 100vh;
    }
`;

export default base;
