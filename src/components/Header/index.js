import React from 'react';

import * as Styled from './styles';

const Header = () => (
    <Styled.HeaderContainer>
        <Styled.HeaderText>React Example Project</Styled.HeaderText>
    </Styled.HeaderContainer>
);

export default Header;
