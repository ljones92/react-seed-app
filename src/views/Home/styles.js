import styled from '@emotion/styled/macro';

// eslint-disable-next-line import/prefer-default-export
export const Home = styled.div`
    text-align: center;
    margin: ${props => props.theme.spacing.xlarge}
        ${props => props.theme.spacing.xlarge} 0;
`;
